// frontend/js/config.js
// The backend is now a separate app/origin (see ../../backend). Point this at
// wherever it's actually running — defaults to the local dev port.
// For a real deployment, replace this with your deployed backend's URL, e.g.
// window.STUDY_HELPER_API_BASE = "https://study-helper-api.example.com";
window.STUDY_HELPER_API_BASE = window.STUDY_HELPER_API_BASE || "http://localhost:3000";
